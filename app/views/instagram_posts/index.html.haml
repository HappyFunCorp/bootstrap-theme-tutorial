.container
  - @instagram_posts.each do |post|
    .row
      .col-md-offset-3.col-md-6
        .thumbnail
          = link_to post.link do
            = image_tag post.standard_url

          .caption
            %p
              When
              = time_ago_in_words post.created_at
            %p
              Liked by:
              - post.instagram_likers.each do |user|
                = link_to user.username, posts_for_user_path(user)
            %p
              Comments by:
              - post.instagram_commentors.each do |user|
                = link_to user.username, posts_for_user_path(user)

          